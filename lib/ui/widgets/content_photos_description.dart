import 'package:flutter/material.dart';
import 'package:airplane/shared/theme.dart';

class ContentPhotosDescription extends StatelessWidget {
  const ContentPhotosDescription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
